<%= form_with(model: reservar, local: true) do |form| %>
  <% if reservar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reservar.errors.count, "error") %> prohibited this reservar from being saved:</h2>

      <ul>
      <% reservar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.number_field :usuario_id, id: :reservar_usuario_id %>
  </div>

  <div class="field">
    <%= form.label :espacio_comun_id %>
    <%= form.number_field :espacio_comun_id, id: :reservar_espacio_comun_id %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.text_field :fecha, id: :reservar_fecha %>
  </div>

  <div class="field">
    <%= form.label :t_inicio %>
    <%= form.time_select :t_inicio, id: :reservar_t_inicio %>
  </div>

  <div class="field">
    <%= form.label :t_fin %>
    <%= form.time_select :t_fin, id: :reservar_t_fin %>
  </div>

  <div class="field">
    <%= form.label :estatus_reserva_id %>
    <%= form.number_field :estatus_reserva_id, id: :reservar_estatus_reserva_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
